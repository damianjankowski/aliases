#!/bin/bash
# ---- Homebrew aliases ----
alias brewup="brew update && brew outdated"
alias brewupg="brew upgrade && brew cleanup"
alias fullbrew="brew update && brew upgrade && brew cleanup && brew doctor"
alias brewuninstall="brew uninstall --zap"
